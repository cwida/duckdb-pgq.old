# name: benchmark/micro/sql-pgq/msbfs.benchmark
# description: MSBFS 
# group: [sql-pgq]

name MSBFS
group sql-pgq

init
PRAGMA threads=4;

load benchmark/micro/sql-pgq/snb.sql

run
SELECT max(reachability(id, v_size, src, dst)) FROM src_dest ;

result I
1